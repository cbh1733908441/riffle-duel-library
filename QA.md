# 检查记录

2026-09-23：数据检查通过，确认两平台标识不冲突、来源和模式关联完整、合作与团队卡片未进入已评级独立对抗、统计按游戏去重、Roblox 未将服务器人数当作模式人数。

在本地浏览器检查了：
- 初始默认只显示独立对抗。
- Roblox + 高适配组合返回 3 个模式；Blade Ball 详情显示四步、NPC 行为与设计限制。
- 原作来源定位与单游戏导出按钮反馈正常；浏览器自动化的下载事件未回传，因此没有据此声称验证文件落盘。
- 分页从 1–24 到 25–48；完整目录显示 8,749 条。
- 390px 窄屏无横向溢出。

`tests/ui.cjs` 是可重跑的浏览器回归检查；本次人工浏览器检查与数据检查不依赖安装它的环境。

## 2026-09-23：策划与参考图生成

- 本机 Codex 0.155.0-alpha.16；原生 image_gen 暴露可用，未暴露型号选择参数。
- 文字默认 GPT-6 Sol / medium，已有 ChatGPT 登录；不使用图片 API 或 ChatCut。
- HTTP 与流程回归：错误来源/Host/令牌拒绝、错误游戏与跨游戏模式拒绝、同请求去重、并发任务限制、图片失败保留策划、仅重试图片（4 组测试通过）。
- 原库校验通过：标识、来源、平台分离、独立对抗范围、人数未知处理。
- 浏览器：Phantom Ball 的「生成策划」实际点击启动；页面刷新后恢复运行中状态；390px 窄屏无横向溢出；生成页无控制台错误。
- 真实端到端任务 `b6d31f91437e489288d2d59be5c1f07b`：2433 个中文字的《幻影回击》策划已完成；首轮生图遇到原生通道网络错误，实际点击「重试参考图」后成功。重试前后策划 SHA-256 相同。
- 参考图已人工查看：一名玩家、一名明确标记的 NPC、一颗指向玩家的球、格挡按钮；PNG 1672×941，源图与项目副本 SHA-256 一致。图片为概念参考，未制作可运行游戏。
- 真实图片通道为 Codex 原生 image_gen；无法核实具体型号，不声称使用了指定 GPT Image 2.5。
- 发布成果只复制文档、图片和输入快照；未发布 CLI 日志、账号凭据或本地会话令牌。

## 生图提示词配置

- 新增按游戏模式保存的完整提示词编辑器、恢复默认、实际输入预览。
- 后端接收并保存模板快照，任务去重包含模板；失败重试可更新模板且保留旧版本。
- 测试覆盖自定义模板进入图片步骤、无占位符自动附上策划、策划原文占位符不被误替换、空白及超长拒绝、长中文 HTTP 提交、重试更新模板；5 组回归通过。
- 浏览器验证：编辑后刷新恢复同一模式的内容、恢复默认成功、展开实际提示词成功；编辑区已做视觉检查。

## 2026-09-23：更新默认生图模板

- 仓库与网页默认模板逐字替换为用户提供的手机版 9:16、英文界面版本。
- 编译验证：完整策划只插入一次，新视觉约束占位符正确填入；新版不带旧的 16:9 要求。6 组生成回归与原库校验通过。
- 配置迁移验证：仅旧默认全文（忽略首尾空白）匹配才迁移，自定义内容保留；恢复默认清除覆盖，后续可跟随默认模板更新。
- 本机浏览器：旧默认自动变为新版；实际编辑、刷新保留、恢复默认成功。
- 本次仅更新提示词配置，未重新调用图片生成，既有成果和任务快照保留。

## 2026-09-23：三步工作台与策划编辑

- 页面按「选择选题 → 编辑策划 → 生成参考图」分步展示；写作时收起选题树，正文与修改侧栏并排，手机端改为上下排列。
- 回归 11 组通过：初稿仅生成文字、章节草稿读取、初稿与手改冲突保护、修改排队、版本校验与恢复、显式生图及版本快照、候选未处理时拦截生图、生图失败保留、默认模板、HTTP 授权与路径校验。
- 真实本机验证使用 TowerFall Ascension 的本地弓箭对抗：CLI 实际逐节更新草稿；生成期间侧栏提交要求进入队列；手动修改、保存、刷新恢复、历史版本回退通过。
- 窄屏实测宽度 375px，无横向溢出；策划生成期间图片按钮禁用，提示词仍可配置。桌面正文与修改侧栏已人工视觉检查。
- 刷新保持所处步骤；编辑采用 Markdown 正文，章节预览不是逐 token 流。生成与修改使用本机 Codex 登录。

## 移除误导性修改侧栏

- 删除右侧修改输入、引导文案和聊天记录展示，正文改为单栏；清除相关事件和样式，避免访问已移除元素。
- 保留直接编辑、自动保存、历史版本、独立生图，以及已有修改稿的处理入口。

## 恢复修改侧栏，清除测试示例

- 恢复修改输入、提交和聊天状态，初始输入与对话记录均为空；移除示例和术语引导，保留简短操作说明。
- 只移除本机验证任务中明确识别的测试消息，已生成策划、历史版本及其他用户内容保留。
